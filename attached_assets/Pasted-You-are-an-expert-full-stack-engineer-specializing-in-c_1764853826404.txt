You are an expert full-stack engineer specializing in creating complete production-grade systems using Next.js 15, TypeScript, Tailwind CSS, ShadCN/UI, Framer Motion, MongoDB, and RESTful API architecture.
Your task is to generate a fully functional, end-to-end full-stack project including both frontend and backend.

🔶 PROJECT REQUIREMENTS
1. Framework & Libraries

Use the following stack:

Next.js 15 (App Router)

TypeScript

Tailwind CSS

ShadCN/UI

Framer Motion (smooth animations everywhere)

MongoDB (Mongoose ODM)

JWT Authentication (optional but well-structured)

Zod validation

Zustand or Context API for state mgmt

Axios or built-in fetch for API calls

🔶 2. Folder Structure

Generate a clean professional project structure:

/app
   /admin
       /dashboard
   /menu
   /checkout
   /api
       /auth
       /categories
       /subcategories
       /items
       /orders
/components
/hooks
/lib
/models
/styles
/utils


Everything must be organized, clean, and scalable.

🔶 3. UI/UX Requirements (Premium Quality)
Styles:

Tailwind with modern radius, shadow, spacing

Use smooth animations with Framer Motion

Use shadcn components for buttons, cards, dialogs, forms

For each page:

Add page transitions

Add hover animations

Use clean spacing and typography

Mobile-first responsive layout

Include:

Professional hero section

Beautiful pricing section

Sticky navbar

Footer

Cards with glassmorphism + gradients

Button micro-animations

🔶 4. Backend Requirements

Create full backend logic inside Next.js API routes:

CRUD Routes:

Categories

Subcategories

Items

Orders

Menu fetch

Admin login

Data Models (Mongoose):
Category: title, image
Subcategory: categoryId, title, image
Item: subcategoryId, title, price, description, image
Order: items[], totalPrice, customerName, phone, time
Admin: username, password (hashed)

Include:

Proper try/catch error handling

Validation using Zod

MongoDB connection reuse pattern

JWT tokens for admin

🔶 5. Admin Dashboard Requirements

Generate:

Login page

Dashboard home

Category management (add/edit/delete)

Subcategory management

Item management

Preview menu

Settings

Dark mode toggle

Animated charts using Recharts or Chart.js

UI must be professional with polished animations.

🔶 6. Client-Side App (User Facing Menu)

Pages to generate:

/menu → Show categories → subcategories → items

/item/[id] → Detailed modal

/checkout → Cart, summary, invoice

WhatsApp order message builder

"Call Now" button

Sticky cart icon

Order success animation

Use:

Zustand for cart state

Framer Motion for page transitions

Toast notifications

🔶 7. WhatsApp Checkout Logic

On checkout:

Generate WhatsApp message:

Customer Order:
Item 1 x 2 - ₹200
Item 2 x 1 - ₹150

Total: ₹350


Redirect to:

https://wa.me/<PHONE>?text=<ENCODED_TEXT>

🔶 8. Deployment-Ready Output

Ensure:

.env.example included

Environment variables documented

MongoDB connection instructions

Build instructions for:

Netlify

Vercel

Render

No hardcoded URLs

🔶 9. Delivery Format

When generating the project, output in the following sequence:

(A) Project folder structure
(B) Package.json
(C) Complete code for every file
(D) Styled UI components
(E) Animations & helper hooks
(F) API routes + models
(G) Admin dashboard
(H) User-facing menu + checkout
(I) Environment variable instructions
(J) Deployment steps

Ensure every page is function