# Menu Fix TODO

## Completed Tasks
- [x] Updated Dish model to use `subCategory` instead of `category`
- [x] Updated createDish controller to use `subCategory` field
- [x] Updated updateDish controller to use `subCategory` field
- [x] Updated listDishes controller to populate `subCategory` instead of `category`
- [x] Updated deleteCategory controller to check for subcategories instead of dishes

## Next Steps
- [ ] Test the backend API to ensure menu data is fetched correctly
- [ ] Verify that the menu displays items in the frontend
- [ ] Check for any console errors or issues
